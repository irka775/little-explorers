"""
App initialization for the checkout application.

This module ensures that the custom `CheckoutConfig` is used when the app is loaded.
"""

default_app_config = "checkout.apps.CheckoutConfig"
"""Specifies the default application configuration for the checkout app."""
